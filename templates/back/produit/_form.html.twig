{{ form_start(form) }} 
    {{ form_row(form.titre) }}
    {{ form_row(form.description) }}
    {{ form_row(form.photoFile) }}
    {{ form_row(form.prix) }} 
   
    
    
    
    {# container qui recevra tous les formulaires por ajouter les etapes#}
    {# Danschaque formulaire on a ce qu'on appelle un prototype qui contient toutes les donnees du formulaire et que l'on peut utiliser pour construire le formulaire au niveau html #}
        <div class="container-etapes" data-prototype="{{ form_widget(form.etapes.vars.prototype)|e('html_attr') }}">
            {# boucle pour afficher tous les champs de mon formulaire photo ProduitType #}
            {% for row in form.etapes %}
                <div class="card-etape">
                    {{ form_widget(row) }}
                </div>
            {% endfor %} 
        </div>

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

